# SCUMM sound IDs registered as TAD SFX.
# Order controls TAD SFX index (first id -> index 0).
# Re-run tools/audio/register_sfx.py after editing.
#
# Trimmed to the minimum set — only SCUMM 99 fires during the currently
# accessible boot/intro path (verified via runtime trace of op_startSound).
# Every other SFX sat unused in CAD, starving the music samples of budget.
# Music quality takes precedence over SFX quantity until the game can
# actually reach rooms where the other SFX would trigger.
99  # PCM=13246 B (BRR~7417 B) soun_099_room070.bin
