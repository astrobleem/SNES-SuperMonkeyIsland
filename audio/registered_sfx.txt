# SCUMM sound IDs registered as TAD SFX.
# Order controls TAD SFX index (first id -> index 0).
# Re-run tools/audio/register_sfx.py after editing.
#
# IDs are DSOU sound ids (the ids scripts pass to startSound) — the
# pre-2026-07 extraction numbered files by disk order instead, which is
# why this entry used to be called 99.
#
# NOTE: with the id map corrected, the boot/intro path fires ids 99/110
# (silent stubs in MIDI mode) — this SBL (172, storage room 70) is kept
# registered as the sole TAD SFX until a real runtime sweep decides the
# SFX roster. Music quality takes precedence over SFX quantity.
172  # PCM=13246 B (BRR~7417 B) soun_172_room070.bin
